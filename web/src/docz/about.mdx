---
name: Inicialização do projeto
route: /initialize
---

# 📦️ Inicialização do projeto

## Clonar o projeto

Para clonar o projeto, use os comandos abaixo:

``` bash
    # Clonar o repositório
    ❯ git clone https://github.com/Henrique-Castro/ManageEXP.git

    # Entrar no diretório
    ❯ cd ManageEXP
```

---

## Rodar o projeto

*Para rodar o projeto é necessário ter [yarn](https://classic.yarnpkg.com/en/docs/install/#windows-stable) ou [npm](https://www.npmjs.com/get-npm) instalado*

### Backend

``` bash
    # Entrar no repositório do Backend
    ❯ cd ManageEXP.Solution

    # Entrar no diretório
    ❯ cd ManageEXP
```

### Frontend

#### Usando yarn

```bash
    # Entrar no diretório web
    > cd web

    # Instalar as dependêcias
    > yarn

    # Iniciar o projeto
    > yarn start
```

#### Usando npm

*Se você usar [npm](https://www.npmjs.com/get-npm), delete o arquivo yarn.lock para ver as dependências da melhor forma.*

```bash
    # Entrar no diretório web
    > cd web

    # Instalar as dependêcias
    > npm install

    # Iniciar o projeto
    > npm start
```

Após a iniciar o projeto localmente acesse [http://localhost:3000]().
